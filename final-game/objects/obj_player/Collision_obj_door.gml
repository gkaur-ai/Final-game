// DOOR COLLISION WITH PLAYER 
if (place_meeting(x, y, obj_player.large_size)) {
	if (obj_player.target_scale >= obj_player.large_size ) { 
	//Let the player pass through 
	}	
}
		 //Player is too small 
		if (place_meeting(x, y, obj_player.small_size))
		move_speed = 0; // Stop the player movement
	
	



